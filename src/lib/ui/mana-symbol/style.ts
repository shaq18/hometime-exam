import styled from '@emotion/styled';

const Icon = styled.i`
  font-size: 16px;
  &:not(:last-child) {
    margin-right: 0.25rem;
  }
`;

const NoMana = styled.p`
  margin-top: 5px;
`;

export const S = { Icon, NoMana };
